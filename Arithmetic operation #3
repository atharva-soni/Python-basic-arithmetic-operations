from fractions import Fraction

def operate(a, b, c, d):
    return (Fraction(a,b) - Fraction(c,d))

a = int(input('Enter 1st number for first fraction: '))
b = int(input('Enter 2nd number for first fraction: '))

c = int(input('Enter 1st number for second fraction: '))
d = int(input('Enter 2nd number for second fraction: '))

print(f'The Answer of Fraction({a}, {b}) - Fraction({c}, {d}) is {operate(a,b,c,d)}')



Output : 

Enter 1st number for first fraction: 
1
Enter 2nd number for first fraction: 
2
Enter 1st number for second fraction: 
3
Enter 2nd number for second fraction: 
6
The Answer of Fraction(1, 2) - Fraction(3, 6) is 0
